export class Hero {
  id: number;
  name: string;
}

/**
notes:
- when using @Input, the child has to be in the same root as parent.
- Inputs are key to passing info from object to object.
- the binding [] is needed for inputs.
*/
